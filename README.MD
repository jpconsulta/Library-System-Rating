
# Documentation





## Download and extract file
## `composer install` to install files
## Use `.example.env` to setup own `.env` file

```
# DATABASE SETUP
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=feedbacksystem2
DB_USERNAME=root
DB_PASSWORD=
```
### Run `php artisan key:generate`

### php artisan make:seeder UsersTableSeeder